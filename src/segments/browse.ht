import "module:std" as std

var Future = std.Future

class BrowseEndpoint {
  construct() {}

  fun charts() {
    return Future.resolve({"items": []})
  }

  fun newRelease() {
    return Future.resolve({"items": []})
  }
}

export { BrowseEndpoint }
