import "module:std" as std

class PlaylistEndpoint {
  construct() {}

  fun getPlaylist(id) {
    return std.Future.resolve({
      "id": id,
      "name": "Playlist " + id,
      "artwork": "https://i.ytimg.com/vi/dQw4w9WgXcQ/mqdefault.jpg",
      "items": [],
      "total": 0
    }.toJson())
  }

  fun save(id) {
    return std.Future.resolve({"status": "success"}.toJson())
  }

  fun unsave(id) {
    return std.Future.resolve({"status": "success"}.toJson())
  }
}

export { PlaylistEndpoint }
