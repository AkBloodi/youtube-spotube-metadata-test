import "module:std" as std

var Future = std.Future

class UserEndpoint {
  construct() {}

  fun me() {
    return Future.resolve({"id": "", "name": "Guest"}.toJson())
  }
}

export { UserEndpoint }
